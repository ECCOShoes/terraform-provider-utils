# Examples

This directory contains usage examples for the `utils` provider functions.

- **provider/provider.tf** — provider configuration
- **functions/** — examples for each provider function (`deepmerge`, `readsops`, `jsonexpand`, `jsonflatten`)
